# Test configuration - disable Flyway for tests
# Tests use Hibernate ddl-auto to create schema from entities
spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
